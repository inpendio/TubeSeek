import { StyleSheet } from 'react-native';
import { colors } from 'config';

export default StyleSheet.create({
  textBase: { paddingHorizontal: 15, paddingVertical: 10 },
  textActive: { color: colors.white, backgroundColor: colors.secondary },
});
